 <section class="login-section mt-30 mb-15">
    <div class="container " >
        <div class="sign-up flex">
            <div class="sign-up__page-info mt-20">
                <h2>{{signUpData.heading}}</h2>
                <p>{{signUpData.subHeading}} </p>
            </div>
            <div>
                <form action="#" autocomplete="off">
                     {{#each signUpData.inputs}}
                    <fieldset>
                        <input id="{{this.id}}" type="{{this.type}}" name="{{this.id}}" required>
                        <label for="{{this.id}}">{{this.label}}</label>
                        <div class="after"></div>
                    </fieldset>
                     {{/each}}
                   
                    <fieldset>
                        <button>{{signUpData.submitLabel}}</button>
                    </fieldset>
                </form>
            </div>
        </div>
    </div>
</section>